# CLAUDE.md

Never use em dashes or hyphens/dashes as punctuation when writing or generating written text (prose, copy, commit messages, comments, etc). Use commas, periods, or separate sentences instead. Hyphens in compound words (e.g. "mother-in-law") or in code/identifiers are fine.
